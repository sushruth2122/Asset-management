/**
 * TopNav – DEPRECATED. Kept as a no-op for backward compatibility.
 * Navigation is now provided by AppLayout (sidebar + header).
 * Pages should use <AppLayout> instead.
 */
export default function TopNav() {
  return null;
}
